# Fase 1 — Segurança: Credenciais e RLS — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mover credenciais do Supabase para variáveis de ambiente e verificar/habilitar RLS em todas as tabelas principais.

**Architecture:** Dois grupos independentes: (1) substituir constantes hardcoded em `client.ts` por `import.meta.env`, tipificar no `vite-env.d.ts`; (2) inspecionar o estado RLS via Supabase e criar migrations documentando/corrigindo as políticas.

**Tech Stack:** Vite (import.meta.env), TypeScript, Supabase MCP (execute_sql), SQL migrations

---

## Task 1: Variáveis de ambiente

**Files:**
- Modify: `src/integrations/supabase/client.ts`
- Modify: `src/vite-env.d.ts`
- Create: `.env`
- Create: `.env.example`
- Modify: `.gitignore`

- [ ] **Step 1: Criar `.env` na raiz do projeto**

```
VITE_SUPABASE_URL=https://pttidjztgnqcyrsreygn.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InB0dGlkanp0Z25xY3lyc3JleWduIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzI4MjgwNzYsImV4cCI6MjA4ODQwNDA3Nn0.kWxbMyfg_kVkGWiRsSjqKBiMgcD2JdpsaFvHIolKBc8
```

- [ ] **Step 2: Criar `.env.example` na raiz**

```
VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
```

- [ ] **Step 3: Adicionar `.env` ao `.gitignore`**

No final do arquivo `.gitignore`, adicionar:
```
.env
.env.local
```

- [ ] **Step 4: Atualizar `src/vite-env.d.ts`**

Substituir o conteúdo atual por:
```ts
/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_SUPABASE_URL: string;
  readonly VITE_SUPABASE_ANON_KEY: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}
```

- [ ] **Step 5: Atualizar `src/integrations/supabase/client.ts`**

Substituir o conteúdo atual por:
```ts
// This file is automatically generated. Do not edit it directly.
// Credentials are read from environment variables (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY).
// If this file is regenerated by Dyad, re-apply this change.
import { createClient } from "@supabase/supabase-js";

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;
const SUPABASE_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY;

if (!SUPABASE_URL || !SUPABASE_ANON_KEY) {
  throw new Error(
    "Variáveis de ambiente VITE_SUPABASE_URL e VITE_SUPABASE_ANON_KEY são obrigatórias. " +
    "Crie um arquivo .env na raiz do projeto com esses valores."
  );
}

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
```

- [ ] **Step 6: Verificar que `.env` não aparece no git**

```bash
git status
```

Esperado: `.env` **não** aparece na lista de arquivos modificados/não rastreados. `.env.example` aparece como novo arquivo.

- [ ] **Step 7: Verificar que o app ainda funciona**

```bash
cd "/Users/pap/dyad-apps/EcoBuzios - financeiro Projetos" && npm run dev
```

Esperado: Vite inicia sem erros. Abrir o browser e confirmar que a tela de login carrega normalmente.

- [ ] **Step 8: Commit**

```bash
git add src/integrations/supabase/client.ts src/vite-env.d.ts .env.example .gitignore
git commit -m "feat: mover credenciais Supabase para variáveis de ambiente"
```

---

## Task 2: Verificar e habilitar RLS nas tabelas

**Files:**
- Create: `supabase/migrations/20260327000000_enable_rls.sql`

As tabelas identificadas no projeto são:
`projects`, `budgets`, `budget_categories`, `budget_lines`, `balancete`, `transactions`, `transaction_attachments`, `vendors`, `invoices`, `orcamentos_importados`, `rubricas_orcamento`

- [ ] **Step 1: Verificar estado atual do RLS via Supabase MCP**

Executar via `mcp__71c9ae92-cabe-4edd-999a-d468df413398__execute_sql` no projeto Supabase:

```sql
SELECT
  schemaname,
  tablename,
  rowsecurity
FROM pg_tables
WHERE schemaname = 'public'
ORDER BY tablename;
```

Anotar quais tabelas têm `rowsecurity = false`.

- [ ] **Step 2: Verificar policies existentes**

```sql
SELECT
  schemaname,
  tablename,
  policyname,
  permissive,
  roles,
  cmd,
  qual
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

Anotar quais tabelas têm policies e quais estão sem nenhuma policy.

- [ ] **Step 3: Criar pasta de migrations**

```bash
mkdir -p "/Users/pap/dyad-apps/EcoBuzios - financeiro Projetos/supabase/migrations"
```

- [ ] **Step 4: Criar migration com estado atual e habilitação de RLS**

Criar `supabase/migrations/20260327000000_enable_rls.sql` com base nos resultados dos passos anteriores.

Para cada tabela com `rowsecurity = false`, incluir:

```sql
-- Habilitar RLS em todas as tabelas principais
ALTER TABLE public.projects ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.budgets ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.budget_categories ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.budget_lines ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.balancete ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.transactions ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.transaction_attachments ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.vendors ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.invoices ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.orcamentos_importados ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.rubricas_orcamento ENABLE ROW LEVEL SECURITY;
```

Para tabelas sem nenhuma policy, adicionar policy básica de acesso autenticado (permite acesso a qualquer usuário autenticado — adequado enquanto o sistema é single-tenant):

```sql
-- Projects: acesso por usuário autenticado
CREATE POLICY "acesso_autenticado" ON public.projects
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.budgets
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.budget_categories
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.budget_lines
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.balancete
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.transactions
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.transaction_attachments
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.vendors
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.invoices
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.orcamentos_importados
  FOR ALL TO authenticated USING (true);

CREATE POLICY "acesso_autenticado" ON public.rubricas_orcamento
  FOR ALL TO authenticated USING (true);
```

> **Nota:** Se o Step 2 revelar que algumas tabelas já têm policies, omitir o `CREATE POLICY` correspondente para não gerar conflito.

- [ ] **Step 5: Aplicar a migration via Supabase MCP**

Executar o conteúdo do arquivo `20260327000000_enable_rls.sql` via `mcp__71c9ae92-cabe-4edd-999a-d468df413398__apply_migration`.

- [ ] **Step 6: Verificar que RLS está ativo**

```sql
SELECT tablename, rowsecurity
FROM pg_tables
WHERE schemaname = 'public'
ORDER BY tablename;
```

Esperado: todas as tabelas com `rowsecurity = true`.

- [ ] **Step 7: Verificar que o app ainda funciona após RLS**

Abrir o app no browser, fazer login e navegar pelas páginas principais:
- Dashboard
- Projetos
- Balancete
- Execução

Esperado: dados carregam normalmente sem erros 403/permission denied.

- [ ] **Step 8: Commit**

```bash
git add supabase/migrations/20260327000000_enable_rls.sql
git commit -m "feat: habilitar RLS em todas as tabelas públicas"
```

---

## Critérios de aceite finais

- [ ] `git status` não mostra `.env`
- [ ] `.env.example` está no repositório com placeholders
- [ ] `client.ts` lê de `import.meta.env` e lança erro descritivo se vars ausentes
- [ ] `vite-env.d.ts` tipifica `VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY`
- [ ] RLS habilitado em todas as tabelas públicas
- [ ] `supabase/migrations/` existe com pelo menos um arquivo
- [ ] App funciona normalmente após todas as mudanças
